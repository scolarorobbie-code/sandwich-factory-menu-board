# React Native / Expo monorepos commonly need this: npm's strict peer-dependency
# resolution rejects valid Expo dependency trees. legacy-peer-deps lets the
# install proceed the way Expo/Yarn-style resolution expects.
legacy-peer-deps=true
